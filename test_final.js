const { normalizeMathExpression } = require('./dist/server.cjs').__esModule ? require('./dist/server.cjs') : require('./dist/server.cjs');
// Can't run it this way directly because it's not exported from server.cjs.
